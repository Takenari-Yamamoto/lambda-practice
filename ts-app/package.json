{
  "name": "ts-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/src/index.js",
    "start:projectA": "ts-node src/index.ts",
    "dockerBuild:projectA": "docker build -f Dockerfile -t project-a-lambda ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.3.12",
    "@swc/core": "^1.4.16",
    "@types/aws-lambda": "^8.10.137",
    "@types/node": "^20.12.7",
    "esbuild": "0.20.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "dayjs": "^1.11.10"
  }
}
